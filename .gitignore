# Demo-/Rohdaten sind regenerierbar bzw. vertraulich -> nicht einchecken
data/
# Kennzahlen-CSV wird pro Lauf neu erzeugt (der Bericht in docs/analysis bleibt)
results/
__pycache__/
*.pyc

# lokale venvs (nicht versioniert)
.venv/

# Privatdaten (Heimkoordinaten, API-Keys) — niemals versionieren
config.local.json
analysis/config.local.json
